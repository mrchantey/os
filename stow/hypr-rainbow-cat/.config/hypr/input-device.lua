-- rainbow-cat (desktop) — per-device input overrides.
-- Shared input settings live in the common hypr/input.lua, required before this.

hl.config({
  input = {
    left_handed = true, -- left-handed mouse
  },
})
