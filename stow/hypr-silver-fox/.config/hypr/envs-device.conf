# silver-fox: Intel iGPU drives the Hyprland compositor (Optimus).
# The NVIDIA GTX 1650 Ti is used for CUDA + per-app PRIME render offload, e.g.:
#   __NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia <app>
# Do NOT force LIBVA_DRIVER_NAME=nvidia — Intel handles display + video decode.
